{
  "name": "@songforge/integrations",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test src/*.test.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@songforge/storage": "*"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
